schemaVersion: 1
id: IL-0010
status: failed
utterance: "make the tab switch match the reference: a tall frosted capsule track, selected tab a bright neutral capsule, semibold labels. system control and materials only"
goal: Replace the standard segmented picker with a custom liquid glass tab picker built using SwiftUI system controls and materials.
interpretation: "Replace Picker with an HStack of Buttons in StoryPanel. The track gets `.background(.ultraThinMaterial, in: Capsule())`, and the selected tab gets `.background(.regularMaterial, in: Capsule())` with `.semibold` font for the labels."
keep: []
deny:
  - package.json
  - package-lock.json
  - packages/edraft/src/**/*.test.*
  - src/lib/screenplay/**/*.test.*
change: Build a custom SwiftUI liquid glass segmented control.
budget:
  files:
    - apple/EDraftUI/Sources/EDraftUI/StoryPanel.swift
  symbols: []
  maxFiles: 1
  maxLines: 400
accept: []
assumptions:
  - text: The user wants a custom layout built from standard SwiftUI primitive controls and materials rather than relying on the default Picker styling.
    source: system
    confirmed: true
createdAt: 2026-09-14T15:49:54.088Z
createdBy: mirdora-dev
