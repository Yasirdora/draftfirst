Agreed on the blocker — fix it first, before anything else, as its own lock. Use your scratchpad probe as the fail-before (Return, Backspace at element start, paste, dead key — below a collapsed cut, both layout paths). The planner and the fallback sync must work in on-screen coordinates over laidElements, with hidden elements restored at their indices after the edit. Also cover the same-cause caret misplacement in insertElements/insertActBreak/renumberActCardsIfNeeded. Vibe Check first, wait for my yes.