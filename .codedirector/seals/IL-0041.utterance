Scope it to exactly this: eDraft notes on an FDX document are written into real `<ScriptNotes>` — one note per message, header grammar per RFC §4.1/4.2, Range from IL-0031's counter, kept by IL-0033 on every save. Native eDraft notes no longer save as body Note paragraphs on FDX. (Fountain files stay Fountain — unchanged.) "Your name for notes" asked once at first note (RFC §8) — must work on macOS, which has no Settings panel at all (found in testing today: no gear, no ⌘,). The prompt covers signing; the panel itself is v1 polish. Threads, resolve, word anchors, report: stay postponed. The 3 test notes currently in ~/Documents/sample02.fdx ("testing this one", "test", "see [scene 4]"): strip them as part of this lock's validation, on a copy first, then the real file with my yes. Proofs: note written in eDraft → opens in FD's navigator with author, anchored, never in the script text; no-edit save still byte-identical; all existing keeps hold. Final Draft check by the human before commit, as the RFC's stage protocol says. Draft the lock and show me the Vibe Check.